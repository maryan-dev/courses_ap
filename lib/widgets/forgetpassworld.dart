import 'package:flutter/material.dart';

class Forgetpassworld extends StatelessWidget {
  const Forgetpassworld({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
